name: 1 - Hello World Github Actions

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '30 15 * * *'
  # push: #git push
  #   branch:
  #     - main #trigger on main branch

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello there
        run: echo "hello"